h2 Persons

div
  | Filter:
  input(ng-model='searchText')

table
  tr
    th ID
    th Class
    th(colspan=2) Name
    th Club
    th City
    th Birthday
    th Actions
  tr(ng-repeat="person in persons | filter:searchText")
    td {{person.id}}
    td {{person.class}}
    td {{person.firstname}}
    td {{person.lastname}}
    td {{person.club}}
    td {{person.zip}} {{person.city}}
    td {{person.birthday}}
    td
      i Edit

div(class="legend")


